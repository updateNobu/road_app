<!--No9-->
<% if flash[:notice]%>
 <div>
   <%= flash[:notice]%>
 </div>
<% end %>
<h3>My page</h3>
<p>プロフィール</p>
<%= link_to("ロードマップ", "/user/road/#{@user.id}")%>
<%= link_to("お気に入り", "#")%>
<%= link_to("投稿", "/user/mroadi/#{@user.id}")%>

<h1>プロフィール</h1>
<img src="<%= "/user_images/#{@user.image_name}"%>">
<%= "#{@user.name}" %>
<%= "#{@user.comment}" %>
<%= "#{@user.link}" %>
<%= "#{@user.email}" %>
