<!--NO3-->
<h1>バックエンド</h1>
<% @roadmaps.each do |roadmap| %>
 <div>
  <%= link_to(roadmap.title,"/roadmap/2/#{roadmap.id}" ) %>
  <%= link_to(@user.name,"#" ) %>
  <%= roadmap.created_at%>
 </div>
<% end %>
<%= link_to("投稿", "/roadmap/new") %>